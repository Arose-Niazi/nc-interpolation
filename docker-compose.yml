name: nc-interpolation

services:
  web:
    build: .
    container_name: nc-interpolation-web
    restart: unless-stopped
    networks:
      - proxy
    expose:
      - "80"

networks:
  proxy:
    external: true
